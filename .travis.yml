language: java
sudo: false
jdk:
- oraclejdk8
cache:
  directories:
  - $HOME/.m2
install:
 - ./mvnw clean package -Dmaven.test.skip=false
branches:
  only:
  - master
after_success:
  - mvn jacoco:report coveralls:report